class Solution {
    public int solve(int[] x) {
        Arrays.sort(x);
        int n=x.length;
        int ans=0;
        for(int i=0;i<n;i++){
            if((x[i]+x[n-1])%2==0){
                return ans;
            }
             else ans++;
        }
        return ans;
    }
}
